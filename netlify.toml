[build]
  base = "client/"
  # Install dependencies, then run the build command directly, bypassing package.json
  command = "npm install && npx vite build"
  # The build output is in 'dist' relative to the base directory
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
